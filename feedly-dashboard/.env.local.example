# Feedly API Token (required)
# Get yours from: https://feedly.com/v3/auth/dev
FEEDLY_TOKEN=your_feedly_developer_token_here

# Feedly Stream IDs (required, comma-separated)
# These can be:
# - Feed IDs: feed/http://feeds.feedburner.com/TechCrunch
# - Category IDs: user/{userId}/category/{categoryName}
# - Tag IDs: user/{userId}/tag/{tagName}
# - Board IDs: user/{userId}/board/{boardName}
#
# To find your stream IDs:
# 1. Go to Feedly web app
# 2. Open browser dev tools (F12)
# 3. Go to Network tab
# 4. Navigate to a feed/folder in Feedly
# 5. Look for requests to /v3/streams/contents - the streamId parameter is what you need
#
# Or use the Feedly API:
# curl -H "Authorization: Bearer YOUR_TOKEN" "https://cloud.feedly.com/v3/subscriptions"
FEEDLY_STREAM_IDS=feed/http://example.com/rss,user/xxx/category/Tech

# Default number of top stories to show (optional, default: 20)
TOP_N_DEFAULT=20

# Anthropic API Key for AI-powered summaries (optional)
# Get yours from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Enable smart AI-powered summaries (optional, 0 or 1)
# Requires ANTHROPIC_API_KEY to be set
SMART_SUMMARY=0

# Enable smart title-based grouping (optional, 0 or 1)
# Uses keyword clustering instead of predefined categories
SMART_GROUPING=0
