// Stub for Habitify sync function
// This will be connected to the actual Habitify API later
export async function syncHabitifyData() {
  console.log('Habitify sync not yet configured');
  return { success: false, message: 'Habitify sync not configured' };
}
